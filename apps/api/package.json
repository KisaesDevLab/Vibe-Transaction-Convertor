{
  "name": "@vibe-tx-converter/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "db:migrate": "echo 'db:migrate placeholder — implemented in Phase 3'",
    "db:seed": "echo 'db:seed placeholder — implemented in Phase 3'"
  },
  "dependencies": {
    "@vibe-tx-converter/extractor": "workspace:*",
    "@vibe-tx-converter/exporters": "workspace:*",
    "@vibe-tx-converter/fidir": "workspace:*",
    "@vibe-tx-converter/reconciler": "workspace:*",
    "@vibe-tx-converter/shared": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.16.2"
  }
}
