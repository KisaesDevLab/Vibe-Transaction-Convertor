name: Release

# Skeleton — populated in Phase 30.
# Triggered by tagged releases; publishes signed multi-arch images to GHCR
# with SBOM, OCI labels, and Trivy scan attestations.

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    if: false # disabled until Phase 30 wires it
    steps:
      - run: echo "Release workflow placeholder"
